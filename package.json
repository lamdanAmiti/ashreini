{
  "name": "ashreinu-downloader",
  "version": "1.0.0",
  "description": "Download audio files from ashreinu.app for offline playback",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-puppeteer": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install puppeteer && apt-get update && apt-get install -y chromium",
    "postinstall": "npm run install-puppeteer"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.5.0"
  }
}
