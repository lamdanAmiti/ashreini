{
  "name": "ashreinu-downloader",
  "version": "1.0.0",
  "description": "Download audio files from ashreinu.app for offline playback",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-puppeteer": "npm install puppeteer && npx puppeteer browsers",
    "postinstall": "npm run install-puppeteer"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^22.8.2"
  }
}
